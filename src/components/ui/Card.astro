---
export interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = true } = Astro.props;
---

<div class={`card-modern overflow-hidden ${hover ? 'card-hover' : ''} ${className}`}>
  <slot />
</div>

<style>
  .card-hover {
    cursor: pointer;
  }
</style>
